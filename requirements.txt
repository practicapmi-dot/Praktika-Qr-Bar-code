# === Обучение детектора (нужно для train.py / Colab) ===
ultralytics>=8.3.0          # YOLO11: detect, resume, finetune, export, мультикласс
PyYAML>=6.0                 # configs/*.yaml, data.yaml
# torch/torchvision ставит сам ultralytics под нужную CUDA (в Colab предустановлены).

# === Трекинг (track_crops.py: уникальные кропы через ByteTrack) ===
lap>=0.5.12                 # linear assignment для ultralytics model.track()

# === Подготовка данных и нормализация (Ph1/Ph3/Ph4) ===
numpy>=1.24
opencv-python>=4.8          # cv2: ORB, гомография, warp, threshold (Ph4)
Pillow>=10.0
# prepare_dataset.py зависимостей НЕ требует (чистый stdlib).

# === Декодирование (Ph5) — раскомментировать при работе над декодером ===
# zxing-cpp>=2.2.0          # основной декодер всех 5 типов
# pyzbar>=0.1.9             # fallback QR/1D   (нужен системный libzbar0)
# pylibdmtx>=0.1.10         # fallback DataMatrix (нужен системный libdmtx0)

# === Синтетика (Ph1, генерация недостающих типов) ===
# qrcode>=7.4
# python-barcode>=0.15
# treepoem>=3.24           # PDF417/Aztec/DataMatrix (нужен системный ghostscript)
